# Workout Tracker updates

This repository hosts signed Android update packages and machine-readable update metadata for a personal Workout Tracker app.

It contains no workout data, source code, credentials, or signing keys. Android accepts an update only when its package identity and signing certificate match the installed app.
